# Dependencies
node_modules/

# Local D1 database and wrangler state. Contains password hashes and the
# session-signing secret -- it must never reach a git repo, public or private.
.wrangler/
data/
.dev.vars

# Logs and OS noise
*.log
npm-debug.log*
.DS_Store
Thumbs.db

# Local environment
.env
.env.local
.dev-data/
