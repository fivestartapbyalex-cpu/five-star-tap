{
  "name": "five-star",
  "version": "1.0.0",
  "private": true,
  "description": "Territory map + CRM for an NFC review-tag sales team",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "reset": "node -e \"require('fs').rmSync('data',{recursive:true,force:true});console.log('Wiped data/. The next start creates a fresh admin.')\""
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
